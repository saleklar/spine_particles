const fs = require('fs'); let C = fs.readFileSync('src/FireGenerator.tsx', 'utf8'); let idx = C.indexOf('float stepAlpha = max(0.0, density - alphaThreshold) * 3.5;'); if (idx > -1) { C = C.substring(0, idx) + 'float stepAlpha = max(0.0, density - alphaThreshold) * (3.5 + max(0.0, alphaThreshold) * 10.0);' + C.substring(idx + 59); fs.writeFileSync('src/FireGenerator.tsx', C); console.log('Replaced by index'); } else { console.log('Not found'); }
