const fs = require('fs'); let C = fs.readFileSync('src/FireGenerator.tsx', 'utf8'); C = C.replace(/float stepAlpha = max\\(0\\.0, density - alphaThreshold\\) \\* 3\\.5;/g, 'float stepAlpha = max(0.0, density - alphaThreshold) * 3.5;\\n            float alphaMult = 1.0 + max(0.0, alphaThreshold) * 4.0;\\n            stepAlpha *= alphaMult;'); fs.writeFileSync('src/FireGenerator.tsx', C);
